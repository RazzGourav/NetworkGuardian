# NetworkGuardian — Python dependencies
# Phase 1: SDN controller
ryu
eventlet

# Phase 2: Monitoring agent
# Note: Using SQLite for metrics storage (built into Python)
# instead of InfluxDB for simplicity in this environment
